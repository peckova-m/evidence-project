<%= form_with(model: pojisteni) do |form| %>
  <% if pojisteni.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pojisteni.errors.count, "error") %> prohibited this pojisteni from being saved:</h2>

      <ul>
        <% pojisteni.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :"Název", style: "display: block" %>
    <%= form.text_field :nazev %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
