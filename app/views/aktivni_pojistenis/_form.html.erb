<%= form_with(model: aktivni_pojisteni) do |form| %>
  <% if aktivni_pojisteni.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(aktivni_pojisteni.errors.count, "error") %> prohibited this aktivni_pojisteni from being saved:</h2>

      <ul>
        <% aktivni_pojisteni.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :"Pojištěnec", style: "display: block" %>
    <%= form.select :pojistenec_id, Pojistenec.all.pluck(:id) %>
  </div>

  <div>
    <%= form.label :"Pojištění", style: "display: block" %>
    <%= form.select :pojisteni_id, Pojisteni.all.pluck(:id) %>
  </div>

  <div>
    <%= form.label :"Měsíční částka v Kč", style: "display: block" %>
    <%= form.number_field :castka %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>